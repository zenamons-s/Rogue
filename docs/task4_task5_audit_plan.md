# Аудит состояния проекта и план для заданий 4/5

Этот документ фиксирует фактическое состояние текущего Go-кода (пакеты `src/domain/*`) и служит базой для внедрения:
- задания 4 (presentation/UI/рендер + управление + статистика в UI);
- задания 5 (datalayer/save-load/leaderboard в JSON).

Ключевые выводы:
1. В репозитории реализован только domain-слой (entities/gameplay/generation).
2. Точки входа приложения (`main`) нет.
3. Слои presentation и datalayer в Go-коде отсутствуют.
4. Для задания 4 уже есть полезные доменные модели (уровень, комнаты, коридоры, игрок, враги, предметы), но нет целостной тайловой карты, позиционирования предметов и механики видимости.
5. Для задания 5 нет никакой файловой подсистемы в `src`, JSON DTO тоже отсутствуют.
